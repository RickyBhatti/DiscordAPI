Citizen.CreateThread(function()
    while true do
        Wait(0) -- Change this to be a value from the config file, maybe? Might actually do a thread creation once the net event replies.
    end
end)